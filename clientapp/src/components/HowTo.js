import { Center } from '@chakra-ui/react';
import React from 'react';

export default function HowTo() {
    return (
        <Center h='200px' color={'white'}>
            Once you create an account, you will automatically be logged in and brought to your portal.
            Then you can submit an estimate request, as of now the file upload feature is no properly operating. 
        </Center>
    )
}